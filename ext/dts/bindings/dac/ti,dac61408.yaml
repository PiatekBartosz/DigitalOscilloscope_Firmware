description: "TI DAC61408"

compatible: "ti,dac61408"

include:
  - dac-controller.yaml
  - spi-device.yaml

properties:
  voltage-reference:
    type: int
    required: true
    description: "DAC voltage reference select"

  channel-gains:
      type: array
      required: true
      description: "Array of 8 channel gains (0..3)"

  "#io-channel-cells":
    const: 1
